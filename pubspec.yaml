name: thesis_scanner
description: "Master Thesis BLE Scanner"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  collection: ^1.18.0
  dchs_flutter_beacon: ^0.6.4
  beacon_distance: ^0.0.3
  fl_chart: ^0.69.2
  mqtt_client: ^10.5.1
  logging: ^1.3.0
  permission_handler: ^11.3.1
  trotter: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
